export interface AppOptions {
    create  : boolean,
    read    : boolean,
    update  : boolean,
    delete  : boolean,
    setup   : boolean,
    list    : boolean,
    archive : boolean,
    restore : boolean,
    search  : boolean,
    tag     : boolean,
    sync    : boolean,
    config  : boolean
}